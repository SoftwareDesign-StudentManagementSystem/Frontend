interface Props {
    studentId: number;
    selectedMonth?: string;
    miniview?: boolean;
    canEdit?: boolean;
}
declare const AttendanceList: ({ studentId, selectedMonth, miniview, canEdit, }: Props) => import("react/jsx-runtime").JSX.Element;
export default AttendanceList;
